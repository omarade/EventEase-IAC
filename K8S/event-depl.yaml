apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eventservice
  template:
    metadata:
      labels:
        app: eventservice
    spec:
      containers:
      - name: eventservice
        image: oaddev/eventservice:9b39ef5a13fc1e00a10aa8c5249788d8fb757645
        env:
          - name: RABBIT_MQ
            valueFrom:
              secretKeyRef:
                name: rabbit-mq-secret
                key: rabbit-mq
          - name: JWT
            valueFrom:
              secretKeyRef:
                name: jwt-secret
                key: jwt
          - name: EVENT_DB_CONNECTION_STRING
            valueFrom:
              secretKeyRef:
                name: event-db-secret
                key: db-connection-str
---
apiVersion: v1
kind: Service
metadata:
  name: event-clusterip-srv
spec:
  type: ClusterIP
  selector:
    app: eventservice
  ports:
  - name: eventservice
    protocol: TCP
    port: 80
    targetPort: 80
